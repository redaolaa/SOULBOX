# MongoDB Connection String
# For local MongoDB: mongodb://localhost:27017/soulbox
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/soulbox
MONGODB_URI=mongodb://localhost:27017/soulbox

# Server Port (set by many hosts, e.g. Render, Railway)
PORT=3000

# Required in production. Use a long random string (e.g. openssl rand -base64 32)
JWT_SECRET=your-secret-key-change-in-production

# Set to production when deployed (so the server serves the React build)
NODE_ENV=development
